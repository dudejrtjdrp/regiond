# ── 의존성 ────────────────────────────────────────────────
node_modules/

# ── 세이브 (게임이 돌면서 만드는 것 — 저장소에 담지 않는다) ──
# 배포하면 서버가 알아서 다시 만든다. Render 무료 플랜에서는 재시작마다 사라진다.
saves/

# ── 로그·임시·스크래치 ────────────────────────────────────
*.log
npm-debug.log*
yarn-error.log*
.tmp/
tmp/
scratch/
*.tmp
*.bak
*.orig
*.rej

# ── 환경 비밀 (ANTHROPIC_API_KEY 등은 절대 커밋하지 않는다) ──
.env
.env.*
!.env.example

# ── 도구·에디터·OS ────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
coverage/
.nyc_output/
