# 인수인계 — §17 배치5 (피드백 대응 + 큰 기능 4종)

> 2026-08-05 작성. 이 문서 하나로 다른 세션/계정에서 이어서 작업할 수 있게 정리했다.
> 기준 상태: **`npm test` 326/326 통과**, 이 커밋까지 전부 PC 저장소에 반영됨.

> ## ⚡⚡ 2026-08-06 밤 — 쉘 복구 후 실행 순서 (이 블록이 최신, 아래 ⚡ 블록보다 우선)
> 상태: 배치 E **커밋 완료(289285b)**. 밸런스 절충안 다이얼 **적용 완료·미검증·미커밋**
> (`balance.json` tier3MedianDays [12,22]→[7,15] · `waves.json` basePower 520→470, 각 _note에 근거).
> GDD3 §17-E에 결정 기록됨(실측치 자리 비워 둠). Sprint 는 **다른 세션이 병행 중**(Sprint1 커밋 fc44571,
> Sprint2 진행 중) — 커밋은 반드시 파일 명시 스테이징, Sprint 파일(assign.js·battle·tick·villagers·
> companions·crewpanel·world.js·data/companions.json·world.json 등) 건드리지 말 것.
>
> 1. ✅ lock·tmp_obj 정리 완료 (2026-08-07)
> 2. ✅ 테스트 3파일 39/39 (trade·waves·world2 — waves 는 470 다이얼로 통과)
> 3. ✅ `npm run simulate` **4/4** (2026-08-07, 20판·씨앗42): 티어3 중앙 7일 · 웨이브5 60.0% ·
>    파산 0% · 성녀 격차 25.0%p. ⚠️ 60.0% 는 밴드 아래 경계 — Sprint 전투 커밋 뒤 재실행 권장
> 4. ✅ 문서 반영(GDD3 §17-E 실측치·README·AI활용) 및 밸런스 커밋
> 5. ✅ `npm run harness` 완주(2026-08-07) — check:ui 통과 + 최상위 6묶음 전부 ok·실패 0
>    (러너가 핸들 잔류로 안 죽는 것만 pkill — 결과와 무관). 실측 7분 내(옛 30분 아님).
>    ⛔ `npm run smoke` 는 샌드박스에 크롬이 없어 불가(root 없음·다운로드 허용목록 차단) —
>    **유저 PC 터미널에서 `npm run smoke` 실행 필요**(크롬 없으면 스스로 skip하는 스크립트)
> 6. ⚠️ settlement.test.js 「굶으면 사람이 떠난다」 1건은 Sprint 쪽 회귀 의심(7c680e0 통과 실측) —
>    Sprint 세션에 전달, 이쪽에서 고치지 말 것. (시뮬 상세에서 초반 웨이브 생존율 양상도 크게
>    바뀌어 있었다 — 성녀 없음 1차 0% 등. 체크포인트는 왕국 기준이라 4/4 이지만 Sprint 쪽 참고)
> 참고: 쉘 고장 양상 둘 — (a) Plan9 마운트 소실(재시작 후 15~20분), (b) VM service 시작 실패
> (Sprint 세션과 공유 충돌 의심, 앱 완전 종료 필요). 백그라운드 프로세스는 호출 사이 죽는다 —
> 긴 작업은 포그라운드 ≤170초 조각으로.

> ## ⚡ 2026-08-06 세션 종료 시점 상태 (다음 세션은 여기부터)
> - 커밋 완료: A(49078c1) · B(b474d89) · C(32c4633).
> - ① §17-18b 태그 고정 해제(server/engine/state.js·emotion_day.js, data/balance.json emotionDay.playerTags, test/playtest17g) → **커밋 완료**.
> - ② §17-19 배치 D 클라 전부(아래 D절) + ③ 대화창 D-5(public/js/dialogue.js·css·연결점 3곳, test/playtest17i) → **커밋 완료**.
> - **배치 E 구현 완료 (2026-08-06 저녁, 미커밋)** — 워킹 트리에 있음: goalRemaining any/all 수정(state.js+quest.js, 17j 6종), PROTOCOL §0-P(누락 명령 4종), mock.js 태그 동기화, GDD3 §17 절·README v3.6 정본화. `npm test` **370/370**·check:ui 통과.
> - **다음 세션 첫 일(쉘 고장으로 못 한 것)**: ① `node --test test/playtest17j.test.js` 재확인(마지막 수정 후 미실행) ② `npm test` 전체 ③ 커밋(fix+docs) ④ `npm run harness` 완주(quest.js 수정 재검증) ⑤ `npm run smoke`.
> - **밸런스 체크포인트 2/4 → 유저 결정(2026-08-06): 절충안** — ① 계약 재작성(티어3 도달 7~15일) ② 웨이브 공격력 소폭 하향(±10%대 다이얼)으로 생존율 60%대 회복. 빠른 초반(집들이·영토확대)은 유지. 실행 순서: E 커밋 → 다이얼 조정 → simulate 재실행 4/4 확인 → README 계약표·GDD3 §17-E 갱신 → 커밋.
> - **`docs/탐험기획.md`(§18) 신설** — 잼후 탐험 확장 정본 + D-2(초반 빈 시간)의 설계는 §18-2 링0·§18-7 매핑을 따를 것. 대화창(D-5) 스펙 정본도 §18-6.

## 0. 프로젝트 개요 (처음 보는 사람용)

- Regiond — 엔드리스 정착지 + 타워디펜스 웹 게임. NHN AI 게임잼 제출작.
- Node 22 ESM 서버(`server/`, express+socket.io) + 바닐라 JS 클라(`public/js`, `GM` 네임스페이스, 단일 캔버스).
- **대원칙**: 밸런스 수치는 전부 `data/*.json`(매직넘버 금지) · 서버 권위 · 결정론(월드 난수를 실시간 로직이 축내면 안 됨 — 개인 난수는 `statRng`) · 한국어 「왜」 주석에 ★ § 태그.
- 테스트: `npm test` (node --test). 실행: `npm start` → http://localhost:3000. 개발 패널: 게임 화면에서 Ctrl+` (하루 길이/멈춤/하루 진행/씨앗).
- 문서: `docs/GDD3.md`(설계 정본) · `docs/PROTOCOL.md`(규약) · `README.md`.

## 1. 이번 배치에서 **끝난 것** (§17-1 ~ §17-15)

유저 피드백 22건 + 추가 요청 3건 중 아래를 구현·검증 완료:

| § | 내용 | 주요 파일 |
|---|---|---|
| §17-1 | 각료(농정관 등) 클릭 무반응 — 말풍선이 #cabinet overflow에 잘려 안 보이던 버그. fixed 배치로 수정 | public/js/hud.js |
| §17-2 | 건물 이전 후 옛 자리에 그려지던 버그 — sortedStructures 캐시 서명에 좌표·티어 포함 | public/js/world.js |
| §17-3 | 하단 UI 겹침(각료 바 ↔ 체력판) — 폭 축소 + 좁은 화면에서 체력판을 윗층으로 | public/css/main.css |
| §17-4 | 동물·적 물 진입 금지 — isWaterAt 신설, creatureMayStand·battle moveToward(축 미끄러짐)·pushOutOfTerritory | server/engine/world.js·ecology.js·battle.js |
| §17-5 | 물고기 → **고기**(낚시) — yield/cycleBonus meat 0.7/1.7(열량 등가), 노드 resource=meat, 3장 목표를 「곡물20 또는 고기7」로 | data/skills.json·world.json·chapters.json |
| §17-6 | 집들이 — 잠자리 건물 완공 순간 빈 침상 있으면 주민 1명 즉시 도착(스윙·일틱 양쪽 경로, resident_arrived 이벤트) | server/engine/residents.js(housewarmArrival)·actions.js·tick.js, balance.json arrival.onHousingComplete |
| §17-7 | 다같이 잠자기 — sleepVote 명령, 사람 아바타 전원이 잠들면 하루 즉시 넘김(전투 중 불가). 체력판 옆 [잠자기] 단추 | server/engine/commands.js·server/index.js(apply)·public/js/hud.js |
| §17-8 | 시작 영토 확대(6→9)·티어 반경 전면 확대(9/13/17/22/27/33/38, endless +5)·첫 땅 물 제거(clearWaterAround)·군락 링/시야/조우 반경 재조정 | data/tiers.json·world.json·creatures.json, server/engine/world.js |
| §17-9 | 건물 손일(직접 상호작용) — handWork 명령: 제련소 손제련(철광석4+장작→강재2)·대장간 벼려 팔기(→골드)·우물 두레박(체력+20)·모닥불 불쬐기·성소 기도(사기)·제재소 톱질(공사력)·사냥막 가죽 손질. 전부 buildings.json handWork 데이터 구동. + 제련소·대장간·광산 해금을 9장→8장으로(강재 루트 막힘 해소) | data/buildings.json·balance.json, server/engine/commands.js·structures.js, public/js/structure.js |
| §17-10 | 외교 제안 빈도 0.55→0.8 + 만료 제안 잔존 버그(클라 offers 병합이 만료를 안 거름) 수정 | data/ai_nations.json, public/js/state.js |
| §17-11 | 동료 상호작용 — 동료 클릭 패널(crewpanel.js), [이곳으로 보낸다](commandCompanion 이동 지시·도착 후 대기), [이름·모양새 바꾸기](customizeCompanion) | server/engine/companions.js·commands.js, public/js/crewpanel.js·input.js |
| §17-12 | 걷어내기(clearNode) — 영토 안 숲/딸기/바위/밭터 노드 제거, 잔량 50% 환급(숲 최소 6 목재), 주민 자동 해제, worldDiff removedNodes | server/engine/world.js(removeNode)·commands.js, data/world.json nodes.clear |
| §17-13 | 다리·매립 — 연구 bridgeworks(티어3)/landfill(티어4), placeBridge/placeFill(철로와 같은 끌어놓기, 물 위 전용), 사람·봇은 다리 위를 걷고(짐승·적은 불가), 매립 칸에는 건물·울타리 건설 가능 | server/engine/research.js·structures.js·fences.js, public/js/build.js·world.js·avatar.js |
| §17-14 | **영토 점령(깃발 멀티)** — 개척 깃발(8장 해금, 영토 밖 건설 허용) 3개를 7타일 안에 모으고 건축가+국방대신이 6타일 안에 함께 서면 반경 9 새 영토 편입(최대 4곳, 본영에서 70타일 이내). 성역·건설·시야·울타리가 새 영토에서 전부 동작 | server/engine/claims.js(신규)·world.js(inTerritory)·fog.js, data/world.json territory.claim, data/buildings.json claim_flag |
| §17-15 | 역할군 개성 — 역할이 차 있으면: 건축가 공사 스윙 1.25배·국방대신 민병 1.15배·농정관 주민 채집 1.1배·공장장 공정 1.15배·외교관 교역 마진 3%·성녀 회복 1.3배. 저택·발리스타·대포는 **건축가 필수**(ROLE_REQUIRED) | data/roles.json perk, server/engine/npc.js(rolePerk)+6개 적용점 |

새 명령(전부 서버 화이트리스트 + net.js OUT + PROTOCOL.md 반영): `sleepVote` `handWork` `commandCompanion` `customizeCompanion` `clearNode` `placeBridge` `removeBridge` `placeFill` `removeFill`.
새 테스트: `test/playtest17.test.js`(동료 지시 7) · `17b`(걷어내기·다리 7) · `17c`(점령·역할 8). 기존 테스트 수정: 티어 반경·어로 고기·e2e 인구 단언 등 전부 ★ 주석으로 사유 명시.

## 2. **남은 것** (이어서 할 일 — 우선순위 순)

### A. §17-16 NPC 국가 가시화 — ✅ **완료 (2026-08-06, 새 계정 세션)**
구현 완료·`npm test` 333/333(기존 326+신규 7, playtest17d)·`check:ui` 통과. 커밋 참조.
- visitNation 명령(visitRadius=6, data/world.json towns) + metNations[상대]=tick, ack에 시세표 동봉 + worldState 즉시 재전송(하루 1회 흐름 대기 방지)
- view.js 마스킹: 가격·태그를 `hasDiplomat OR hasMet`로 완화, metTick 신설(부재 원칙). 인구는 외교관 전용으로 유지(의도적 결정)
- 클라: diplomacy.js(방문 모달+E키, 90ms 메모), world.js drawAiTowns(fog≥1, atlas.hall tier2 고정), input.js E키 분기
- PROTOCOL.md §0-R 신설
- ⚠️ 발견된 기존 문제: 이전 배치 명령 4종(sleepVote·handWork·commandCompanion·customizeCompanion)이 PROTOCOL.md에 미기재 — E에서 보충할 것
- ⚠️ harness(약 30분)는 이번에 미완주 — E에서 재검증
- 방문 모달 [교역한다]는 관제 선포 전이면 토스트만 뜸(초반엔 무역 자체가 닫혀 있어 실해는 없음)

### B. §17-17 탐험 확대 + 바이옴·맵 확장 — ✅ **완료 (2026-08-06)**
`npm test` 345/345(+playtest17e 12종)·`check:ui` 통과. 원안 4항목 + 유저 확정 확장:
- cache 숨은 궤(count 100, concealChance 0.55, minTownDistance 18, statRng 결정론 보상 골드 25~70+30% 유물, 영구 소진)
- ruinGradeBoost 미전달 버그 수정(전달+소모 후 리셋, 회귀 2종) · ruins.json 카드 6장 추가
- **맵 256→384**(면적 2.25배, 노드 count 비례 — iron·oil은 도읍 부속이라 제외) · 깃발 멀티 본영 70→110타일·maxClaims 6
- **바이옴 snow·jungle**(RLE codes 끝에 추가, 위도 30% 띠×고도/습도 → 실면적 22.4%, 중심 60타일 보호, water/rock은 keepCodes로 보존) · snow=건설불가/철·석탄 풍부, jungle=숲·딸기 무성
- 바이옴 첫 발견 연대기+사기 보너스(나라별 1회)
- 성능 실측·수리: minimap 안개 루프 프레임당 147k칸→fogDirty 시만 재굽기, world.js 지형 청크 LRU 192장 상한. PROTOCOL 0-Q-2 근거 기록
- ⚠️ 발견(이 배치와 무관): harness 1건 실패 — 4c440af가 chapters.json grain20을 any로 바꿨는데 state.js goalRemaining()이 resource 외 타입에 null → 3장 목표 힌트 안 붙음. E에서 수정할 것.

#### (원안 참고용 — 위로 대체됨)
1. 새 노드 `cache`(숨은 궤, count 44, 은닉 concealChance 0.55, 본영 18타일 밖) — generateNodes에 concealChance/minTownDistance 지원 추가 필요. skills.json nodes.cache(건설 스킬 3스윙) + actions.js에 cacheReward 분기: statRng(`시드:cache:노드id`)로 골드 25~70 + 30% 유물(grantRandomArtifact), 열면 영구 소진.
2. **버그 수정**: 큰 유적의 gradeBoost가 유물 굴림에 안 실림 — actions.js가 nation.ruinGradeBoost를 쌓지만 king.js resolveRuinChoice→grantRandomArtifact에 전달·리셋을 안 함.
3. data/ruins.json 카드 6장 추가(선택지 다양화 — applyRuinEffect가 지원하는 op는 gold/morale/resource/artifactRoll뿐이니 그 안에서 설계).
4. atlas.js에 cache 스프라이트, world.js 동사맵에 `cache: 'E — 궤를 연다'`.

### C. §17-18 감정의 날 확장 — ✅ **완료 (2026-08-06)**
`npm test` 352/352(기존 345 + playtest17f 7종) · `check:ui` 통과.
- `data/tags.json` 전 9종에 `flavor`(한 줄 이야기) + **새 태그 2종**: `quarry`「너덜겅」(gather.stone +0.45, strength) · `rottenvein`「삭은맥」(output.ironOre −0.3 · gather.stone −0.15, weakness)
- **효과 키 전수 조사 결과**(설계 근거): 태그 effects 중 엔진이 실제로 읽는 것은 `economy.tagFactor` 가 보는 **`output.{grain·ironOre·oil·steel·build}`** 와 **`gather.{wood·stone}`** 뿐이다. `defenseTotal`·`populationGrowth` 는 아무도 읽지 않는 죽은 키고, `unlock` 도 실제 문은 `resources.meta.oil.requiresTag` 가 연다. `buildCost` 는 `build_cost.js` 가 **fortress 만 이름으로 박아** 읽는다(일반화 안 됨). 새 태그는 살아 있는 두 키만 썼다.
- `emotion_day.js buildCutscene`: **프레임 = 5(옛 연출) + 태그 수 + 1(마무리)**. 태그 장은 「이름 — flavor」, 빛깔·마무리 문구는 `balance.emotionDay.cutscene` 이 쥔다. 클라 `cutscene.js` 는 프레임 수를 세지 않아(길이 비례 재생) 하드코딩 수정 불필요 — 대신 모달에서 이야기를 다시 읽도록 `tagStories` 를 이벤트에 추가하고 `.tag-stories` 인용문 스타일을 붙였다.
- `templates.ko.json` emotion_day 4→6(`{{tagLine}}`·`{{nodesRevealed}}` 활용). PROTOCOL §3-2b 에 `emotionDay` 페이로드·가변 프레임 수 명시.
- ⚠️ 남긴 것: 태그가 4개면 컷신이 10장(클라 계산상 21초)까지 늘어난다 — 건너뛰기 단추는 있지만 장당 2100ms 는 손대지 않았다. 실제 플레이어 태그는 `state.js` 가 `['fertile','holy']` 로 못 박아 두어 8장(16.8초)이다.
- ⚠️ `assignTags`(무작위 태그 배정)는 플레이어에게 `pendingTags` 가 늘 있어 현재 흐름에서 호출되지 않는다 — 새 태그가 실제 게임에 나오려면 `state.js` 의 고정 태그를 풀거나 AI 국가에 붙여야 한다(별도 판단 필요).

### D. §17-19 표현·타격감 + 대화창 — ✅ **완료 (2026-08-06)**
`npm test` 365/365(352 + playtest17g 4 + 17h 3 + 17i 6) · `check:ui` 통과.
전부 클라(public/) 작업. 서버·프로토콜은 한 줄도 바뀌지 않았다. 수치는 전부 `data/world.json render.*` 로 뺐다.
1. **건설 UI 하단 배치** — `.place-bar` 를 top:10 → bottom:136(반응형 132/188/172)으로, 아래에서 밀려 올라오는 `placeIn` 애니. 같은 자리를 쓰는 선택 패널(`ctx-panel`)과는 build.js·hud.js 가 서로 닫아 주어 한 번에 하나만 뜬다(단, 자리를 놓는 중이면 배치대를 지킨다).
2. **건물 스프라이트 확대 1.18배** — 그림과 클릭 판정이 각자 베껴 쓰던 사각형 식을 `GM.world.structureRect` **하나로 합치고**(input.js 가 그것을 그대로 씀) `render.structureSprite.scale` 로 키웠다. 밑변·가로 한가운데는 붙박이라 커져도 건물이 제자리에 선다. 회귀: `test/playtest17h.test.js`.
3. **업그레이드 외형** — §15-B-4 의 티어 레이어(2단 기단·기둥 / 3단 금장 처마·깃발·등불)가 이미 있었고, **4단 금테**(`goldTrim`)를 더했다. 캐시 키에 티어가 이미 들어 있다.
4. **피격 이펙트** — `hud.hurt()` 한 곳이 흔들림·붉은 섬광·hitStop·깎인 수를 낸다. 들어오는 길은 둘(전투 사건 `playerHit` · 체력 감시 `watchHp`)이고 `render.hit.mergeMs` 안의 두 번째는 삼킨다(한 대에 두 번 흔들리지 않게). 적·짐승은 그 자리만 흰 테+불티(`swing.targetFlash`) — 내가 때릴 때 화면을 흔들면 멀미가 된다.
5. **대화창(D-5)** — `public/js/dialogue.js` 신설(`GM.dialogue.open/close/advance/choose/isOpen/peek`). 하단 중앙 종이판 · 겹침 이름표 · atlas 절차 도트 초상(88px, pixelated) · 타자기 18ms/자(클릭·E·Space 스킵) · `--gold` ▼ 0.8s 깜박 · 선택지 숫자키 1~4 · ESC 닫기 · 전투 경보 자동 닫힘 · 대화 중 하단 HUD fade(`body.dialogue`). 연결점 3곳: 동료 클릭 인사말(crewpanel.greet) · 감정의 날 tagStories(모달 인용문 → 대화창) · 타국 방문 인사(diplomacy.greet). 선택지가 보내는 것은 기존 화이트리스트 명령뿐이다. 회귀: `test/playtest17i.test.js`(jsdom).

⚠️ 남긴 것: ① 대화창의 넷째 연결점(각료 말풍선의 긴 버전, 탐험기획 §18-6)은 손대지 않았다 — 각료 말풍선(§17-1)은 이미 제 자리에서 도는 흐름이라 잼 범위에서 굳이 흔들지 않았다. ② 실브라우저 스모크(`npm run smoke`)로 대화창을 눌러 본 적은 없다(jsdom 회귀 6종까지만). ③ 감정의 날 이야기가 모달 인용문에서 대화창으로 **옮겨 갔다** — `.tag-stories` CSS 는 쓰이지 않는 채로 남겼다(되돌릴 여지).

### D-2. 초반 빈 시간 해소 — ✅ **완료 (2026-08-07, 커밋 1edf7ea)**
탐험기획 §18 링0 최소판 그대로: 보장 사슬 first_tracks(3단계, 결말 딸기 군락/다친 개) + 미시 5종 풀에서 4~6개, `data/trails.json`+`server/engine/trails.js`+`public/js/trails.js`, 출력은 대화창(D-5). investigateTrail 명령(PROTOCOL §0-O — §0-S 는 델타 절이 선점). 흔적은 nodes 가 아니라 `map.trails` 별도 배열, ack 에서 fog 좌표를 지워 마커 금지를 계약으로 잠금. 회귀 playtest18 12종·기존 테스트 무수정. ⚠️ 남긴 것: mock 본격 동기화(잼후), 흔적 위 건물 건설 회피(잼후, buildingPlacement.nodeClearance 자리), 미시 5종 중 판당 4~6개라 다섯 종이 다 나오진 않음(설계).

### E. 마무리 작업 — 🔶 **대부분 완료 (2026-08-06)**
- ✅ E-1 갈래 목표 안내 버그: `state.js goalRemaining` + **`quest.js missingResource`**(같은 뿌리의 둘째 얼굴)를
  `state.goalNeeds()` 하나로 합쳤다. 가까움의 자는 서버 `progression.measure` 와 같은 **찬 비율**.
  회귀 `test/playtest17j.test.js` 6종. `meat` 를 해결 말풍선 자원 표에 추가.
- ✅ E-2 PROTOCOL.md §0-P 신설 — `sleepVote`·`handWork`·`commandCompanion`·`customizeCompanion`
  (실제 구현을 읽고 적었다. 코드는 한 줄도 안 건드림). §3-5 표와 StructureView 에 `handWork` 도 반영.
- ✅ E-3 구경 모드(`mock.js`) 동기화 — 태그를 시드로 뽑고(월드 난수 안 축냄) 컷신 5+태그+1,
  `tagStories`·`nation.tags`(감정 뒤에만)·`cfg.tags` 까지. 네 씨앗으로 실측 확인.
- ✅ E-4 README(v3.6 절·384·티어 반경·건물 39종·테스트 370) · GDD3 §17 절(배치5 표 + A~E) 정본화.
- 🔶 E-5 검증: `npm test` **370/370**(6묶음 분할 실행) · `check:ui` 통과 · `npm run simulate` **2/4**.
  하니스는 첫 묶음(1~6장)만 돌렸고 — E-1 수정 전 2건 실패 → 수정 후 재검증 **미완**(작업 환경의 셸이 끊김).
  나머지 묶음 4개와 `npm run smoke` 도 미실행. **다음 세션에서 `npm run harness` 완주 필요.**
- ⚠️ **밸런스 체크포인트 2/4** — 원인 분해까지 마쳤다(GDD3 §17-E 표): 곡선을 앞당긴 것은 **§17-6 집들이**
  (티어3 12일 → 7일), 웨이브 방어를 얇게 한 것은 **§17-8 영토 확대**(웨이브5 68% → 45%).
  다이얼은 손대지 않았다 — 계약을 새로 적을지, 집들이에 문턱을 줄지는 설계 판단.
- 실브라우저 스모크(Playwright): 잠자기 단추·손일 단추·동료 패널·깃발 점령 E2E. (미착수)
- 유저에게 물어볼 것: 팀원 롤 기술서에 넣을 **김태우 담당 파트**(프로젝트 지침).

## 3. 진행 방법 메모

- 유저 PC 저장소: `C:\Desktop\...\Game\Toji` (Claude 세션에선 device 브리지 마운트 `mnt/Toji`). 마운트에선 rm 불가 → 삭제는 `_to_delete/`로 mv. git 잠금파일(index.lock 등)도 mv로 치우고 작업.
- 파일 반영은 tarball → `tar -xzOf 아카이브 경로 > 경로` 식 개별 덮어쓰기(마운트가 in-place unlink를 막음).
- 커밋 말미: `Co-Authored-By: Claude ...` 트레일러 유지(기존 커밋 참조).
- 유저 게임 반영 시 **서버 재시작(npm start) + 브라우저 Ctrl+F5** 필수 안내.
