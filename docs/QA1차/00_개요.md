# QA 1차 — 분류·우선순위·배치 계획 (2026-08-06)

멀티플레이 QA(방장+초대 유저+봇)에서 나온 피드백 전체를 버그(B)/기능(F)으로 분리해
01~10 카테고리 파일로 정리했다. ID 규칙: `B01-1` = 01 카테고리 버그 1번.

## 대원칙 (모든 배치 공통)
1. 한 번에 다 고치지 않는다 — 아래 배치 순서대로, 배치마다 유저 확인.
2. 기존 기능·저장 데이터·멀티 프로토콜 호환 유지. 계약 변경 시 `docs/PROTOCOL.md` 먼저.
3. 확신 없으면 임의 구현 금지 — 현재 구조 분석 → 최소 변경 리팩토링.
4. 배치마다: 구조 분석 → 구현 → `npm test` 전체 → 상세 커밋 → `docs/AI활용.md` 갱신.
5. 수치는 전부 `data/*.json` — 매직넘버 금지. 밸런스 변경 시 `npm run simulate` 4/4 유지.

## 핵심 가설 ★
01의 버그 대부분은 **「방을 나갔다 같은 코드로 재입장하면 해결」**된다.
→ join 시 풀스냅샷 경로는 정상이고, 이후 **증분 브로드캐스트가 방 전체(io.to(room))가 아니라
방장 소켓(socket.emit)에만 가거나, 게스트가 룸 구독에서 빠져 있는** 계열의 근본 원인일 가능성이 높다.
개별 증상을 하나씩 패치하지 말고 **emit 경로 전수 감사부터** 한다. (01 파일 참조)

## 우선순위
| 순위 | 내용 | 카테고리 |
|---|---|---|
| P0 | 멀티 동기화 붕괴(재입장하면 낫는 계열 전부) | 01 |
| P0 | 이동 끊김·순간이동(보간) | 02 |
| P1 | 기능이 죽어 있는 버그(공장장 UI, 잠듦, 경험치, 회복, 물스폰, 매립, 봇 배치, 타게임 언급) | 03~05, 08 |
| P2 | 진행 흐름(침공 앞당기기·실패 구제·강재 병목·장 확장) | 04, 06 |
| P3 | 콘텐츠 깊이(바이옴·적·무기·건물·연구·무역·금화) | 05, 07~10 |

## 배치 계획
- **QA-A (P0)**: 01 전체. emit 전수 감사 → 일괄 수정. 검증: 2클라 시나리오(입장 순서 바꿔서).
- **QA-B (P0)**: 02 보간 + 시작·입장 직후 순간이동(01 스냅샷 리셋과 연관 확인).
- **QA-C (P1 버그)**: B03-1·2·8, B04-1~3, B05-1·2, B08-2.
- **QA-D (P1 UX)**: F03-3~7·9·10.
- **QA-E (P2 진행)**: F04-4~9, F06-1·2. simulate 재보정 필수.
- **QA-F (P2~3 콘텐츠)**: 05-3·4, 07, 08, 09, 10. 규모 크므로 착수 전 세부 기획 재분할.

## 완료 기준
각 항목에 「수용 기준」이 있으면 그것으로, 없으면 「증상 재현 불가 + npm test 통과」로 판정.

---

## 진행 결과 (2026-08-07 야간 배치 A~F4 완주)

배치마다 「구조 분석 → 최소 변경 → `node --test` 전체 → 커밋 → `docs/AI활용.md` 갱신」을 지켰다.
세이브 판번호(`world.schema` 6)는 한 번도 올리지 않았다 — 모든 신설 칸이 **없으면 기본값**이다.

| 배치 | 다룬 항목 | 커밋 | 처리 |
|---|---|---|---|
| QA-A (P0) | 01 멀티 동기화 — emit 경로 전수 감사 | `7ecbb29` · `3f89624` | 증분 방송이 방 전체에 닿게(아바타 뷰·명부·쓰러짐·지운 노드). 가설(방장 소켓에만 감) 실측 확인 |
| QA-B (P0) | 02 보간·순간이동 | `f71b308` · `a4223c8` | 스냅샷 보간 띠 한 채(`public/js/interp.js`)로 짐승·사람·동료·적을 같은 규칙에 |
| QA-C (P1 버그) | B03-1·2·8, B04-1~3, B05-1·2, B08-2 | `2d52f4f` · `148659a` · `268591a` · `643a798` | 공장장 창·금화 상시·타 게임 언급 제거 · 잠 한 밤 · 나눈 손의 일터 · 물 스폰 |
| QA-D (P1 UX) | F03-3~7·9·10 | `b25d5df` · `d23a39a` | 새 땅·새 날 한가운데 알림 · 얼굴 눌러 이동 · 건물 곁 E 한 번 · 터렛 사격 가시화 |
| QA-E (P2 진행) | F04-4~9, F06-1·2 | `97c7fe2` · `0e3df05` · `d92903c` · `f8a65b5` | 침공 조건 공개·앞당기기 · 실패 구제 · 무쇠 화로 · 방장 시계 · 산출 상향 후 규모 재보정 |
| QA-F1 (P3) | 08 건물 · 10 전투 | `f1103e4` · `188ddce` · `75695ae` · `3b82b1f` · `9352d83` | 건물 체력 이관(MIGRATION_REV 2) · 부수며 진입 · 서리탑/화염탑 · 목장 우리 + Q 키우기 |
| QA-F2 (P3) | 05·07 세계 콘텐츠 | `f64dd79` · `10d8ab0` · `6d60327` · `0897b78` · `4f647a6` · `71d9be4` | 바이옴 13 · 적 4종+섞인 무리 · 세계 유일 용 · 무기 4/방어구 3. 중간에 낡은 사본 반입 사고 → 3-way 병합 복원 |
| QA-F3 (P3) | 07 경제 콘텐츠 | `c2d30f5` · `a92b51a` · `ba3ea5f` · `e747585` | 나라별 시세 차·특산품 6 · 금화 사용처 4 · 부산물 소비 · 감정소 붙박이 · 주민 꾸미기 |
| QA-F4 (P3) | 09 연구·기차 | (이 배치) | 연구 갈래 카테고리 + 연구소 3종 · 정거장 + 기차 왕복·탑승 |

### 보류 — 다음 배치로 넘긴 것

| 항목 | 까닭 |
|---|---|
| 장(章) 수 확장 (F04) | 진행 장부의 세이브 이관이 필요하다 — 배치 안에서 안전하게 끝나지 않는다 |
| 용병 고용 (F07) | 웨이브 밸런스 파급이 커 `simulate` 재보정이 따로 필요하다 |
| 연구 **동시 진행 2건** (F09-1) | `nation.research.active` 가 한 칸뿐이라 세이브 모양과 명령 규약이 함께 바뀐다. 대신 대학당이 전 분야 가속 + 가속 할인으로 같은 값어치를 준다 |
| 기차 **화물** (F09-2) | 사람 태우기까지가 이번 몫이다. 물류 계층(창고↔정거장)의 설계가 따로 필요하다 |
| 세계관 통합 반영 (W1~W4) | `docs/세계관기획.md` v1.2 는 기획 단계 — 개발 배치는 별도 |

### 최종 검증 (배치 F4 끝, 클라우드 복제본 md5 대조)

- `node --test` **45개 파일 전부** 분할 완주 — **491/491 통과**(A~F3 기준선 475 + F4 신설 16)
- `npm run simulate` 시드 42 — **4/4 통과** (티어3 도달 7일 · 웨이브5 60.0% · 식량 파산 0.0% · 성녀 격차 40.0%p)
- 시드 77 — **3/4** (웨이브5 55.0%, 목표 60~80%). **기준선 `e747585` 에서도 같은 값**이라 이번 배치의 회귀가 아니다
- `npm run harness` **52/52** · `npm run check:ui` 통과 · `npm run e2e` 4/4
